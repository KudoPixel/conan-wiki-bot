# -------------------------------------------------------------------
# ENVIRONMENT VARIABLES
# This file contains sensitive API keys and tokens.
# It MUST be excluded from version control (e.g., via .gitignore).
# -------------------------------------------------------------------
# 1. TELEGRAM CONFIGURATION
# The unique token provided by BotFather for your Telegram bot.
TELEGRAM_BOT_TOKEN="YOUR_TELEGRAM_BOT_TOKEN_HERE"

# 2. GEMINI API CONFIGURATION
# The API key required for authenticating requests to the Gemini API.
GEMINI_API_KEY="YOUR_GEMINI_API_KEY_HERE"

# 3. PROJECT ENVIRONMENT
# Used to determine logging level or operational mode (e.g., development, staging, production).
ENVIRONMENT="development"

# 4. Error Logging Chat ID
# The numeric ID of the channel/group where critical errors will be sent.
# Must include the negative sign (e.g., -100123456789).
TELEGRAM_ERROR_CHAT_ID=""